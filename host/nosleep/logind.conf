# Installed by ~/dotfiles/host/provision.sh (nosleep module).
# An agent box never sleeps. The sleep targets are masked as well; these keys
# stop logind from asking for sleep at all. logind reads them at boot.
[Login]
HandleLidSwitch=ignore
IdleAction=ignore
