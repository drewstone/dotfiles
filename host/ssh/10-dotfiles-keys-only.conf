# Installed by ~/dotfiles/host/provision.sh (tools module).
# The account has passwordless sudo, so a guessed or reused password must not
# open a shell. sshd keeps the first value it reads, and this file sorts before
# the other drop-ins and the main sshd_config.
PasswordAuthentication no
KbdInteractiveAuthentication no
