# Installed by ~/dotfiles/host/provision.sh (wifi module).
# Wi-Fi power save off for every profile that does not set its own value.
# Ubuntu ships default-wifi-powersave-on.conf in this directory, and the file
# that sorts last wins, hence the zz- prefix.
[connection]
wifi.powersave = 2
