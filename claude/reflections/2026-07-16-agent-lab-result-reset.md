# Reflection: agent-lab result-first reset

Date: 2026-07-16.

Overall grade: `2/10`.

Agent-lab has `1,309` indexed run rows, `777` pending verdicts, `184` round numbers, and zero completed repository-disjoint learned-versus-control comparisons.
R373 V4 spent an estimated `$8.05635625` on one fixed worker and one author but executed zero learned candidates.
The program built strong execution records and repeatedly corrected false claims, but it substituted attempts, reports, and changing hypotheses for the one causal coding comparison the paper required.

The active research claim is now narrower.
With identical downstream search opportunity and complete cost accounting, the study asks whether source-bound evidence improves one unchanged profile-wide runtime method and whether that method beats exact specialized alternatives at common total-spend checkpoints.
Trace-driven profile editing itself is not novel because GEPA, SkillOpt, FAPO, Agentic Harness Engineering, Meta-Harness, Self-Harness, and related work already cover it.

The reset removed stale active-state prose, replaced the 3,150-line live state and 14,665-line progress log with compact current records, marked historical programs as historical, and made one scored R373 candidate execution the only next step.
Runtime exposes the one public improvement flow, agent-eval supplies the existing profile author and lower-level search, the interface package defines `AgentProfile`, and sandbox materializes profiles.
Agent-lab keeps only source labels plus dispatch and metering adapters and will not build another compiler or proposer.

The current-session trace reported `306` tool calls, `207` subagent spawns, zero task-quality measurements, and a `6:1` corrective-to-positive user-reaction ratio.
It found no exact repeated tool-call loop, so the waste came from breadth and unfinished branches rather than a single literal retry loop.

Durable lessons:

- Count completed comparisons, not attempts, files, calls, or rounds.
- Require one automatically proposed candidate to execute and score before scaling.
- Correct outcomes must beat absent and shuffled outcomes before claiming outcome-conditioned learning.
- A proposed difference is not causal evidence until its changed behavior executes and removing it removes the gain.
- Composition matters only if it beats the best single change and blind composition at common total spend.

Next: `/evolve` targeting one scored R373 candidate, from a baseline of zero candidate task executions.
